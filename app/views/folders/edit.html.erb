<h1 class="mb-5"><i class="fa-solid fa-pen-to-square"></i> Редактиране на <i class="fa-solid fa-folder"></i>
  папка <%= @folder.name %></h1>

<%= form_with model: @folder, local: true do |form| %>
  <div class="form-group mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group mb-3">
    <p>От тук може да изберете директория на папката. Ако оставите полето празно, папката ще бъде създадена в основната ви директория</p>
    <%= form.label :parent_id, "Избери директория, където да бъде създадена папката", class: 'form-label' %>
    <%= form.select :parent_id, options_for_select(nested_folders_options(current_user.folders, nil, 0, @folder), @folder.parent_id), include_blank: true, class: 'form-control' %>
  </div>

  <%= form.submit 'Запази', class: 'btn btn-primary' %>
<% end %>

<%= link_to 'Назад', folders_path, class: 'btn btn-secondary mt-5' %>
