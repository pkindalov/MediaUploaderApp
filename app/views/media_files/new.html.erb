<h1 class="mb-5">Качване на нови файлове:</h1>
<%= form_with model: [@folder, @media_file], local: true, html: { multipart: true } do |form| %>
  <div class="mb-3">
    <%= form.label "Файлове", class: 'form-label' %>
    <%= form.file_field :files, multiple: true, class: 'form-control' %>
  </div>
  <%= form.submit 'Качете', class: 'btn btn-primary' %>
<% end %>
